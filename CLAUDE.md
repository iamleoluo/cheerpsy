# CheerPsy — 給 Claude 的專案規則

## 1 · 專案文件一律放進 `document_reference/V2升級計畫/`

**這是本專案的正式文件體系，不是暫存資料夾。**

任何「給人讀的」產出——規劃、架構說明、進度盤點、回顧、交接、調查報告——
一律寫成該資料夾裡的**下一個編號 HTML**，並更新 `README.md` 的檔案清單表格。

    document_reference/V2升級計畫/
      00_總覽.html
      01_可行性研究與風險.html
      …
      11_前端設計語言與重構規劃.html
      README.md          ← 檔案清單 + 閱讀順序，新增文件必須同時更新這裡

**不要改用其他形式交付**——不要只留在對話裡、不要另建 Markdown、
**不要發布成 artifact**。已經有人告知過兩次了。

### 為什麼

- 這些文件**跟程式碼一起進版控**，改動與文件會在同一個 commit 裡對得起來。
- 診所與工程師是從 `README.md` 那張表進去讀的，不在表上的東西等於不存在。
- 文件之間**互相引用章節編號**（「依 09 §6 的九步驟」「07 §2.1 報價快照」）。
  散落在別處的文件無法被引用，也不會被下一個人讀到。

### 新增文件時

1. 複製任一份既有 HTML 的骨架：單檔自足（inline CSS/JS、無外部相依），
   `<!doctype html>` 開頭、`<body data-doc="NN_檔名">`、紫色 `.hd` 抬頭、
   `.wrap > .cols > nav.toc + main` 版面、TOC 由頁尾 script 自動生成。
2. 沿用既有的 CSS 變數與 class（`.note`／`.tag`／`.tw table`／`.kpi`），不要另立一套。
3. 更新 `README.md`：檔案清單表格加一列（`#`／檔名連結／內容／給誰看）。
4. 若這份文件修訂了既有文件的判斷，**回去把那份也改掉**，不要讓兩份並存互相矛盾。

---

## 2 · 事實要查過再寫

文件裡的每個數字、狀態、檔案路徑都要當場查證，不要憑印象。

實際發生過的錯誤：doc 11 §3.1 把六個合約面板區塊標成「缺」，
但其中四個功能早就做出來了，只是寫在單一檔案裡沒抽成元件。
**「缺功能」與「缺抽象」的處理順序完全不同**，標錯會讓人去重做一次已經能用的東西。

同理，doc 11 第一版在沒讀 07／08 的情況下寫完，三處與既有裁示牴觸，整份重寫。
**動筆前先把相關文件讀完**，尤其 07–10。

---

## 3 · 驗證指令

```bash
# 後端測試（目前 243，只增不減）
cd apps/api && DATABASE_URL=postgresql://cheerpsy:cheerpsy@localhost:5432/cheerpsy_test \
  .venv/bin/python -m pytest -q

# 跨表不變量（目前 25 條，全綠才算數）
cd apps/api && DATABASE_URL=postgresql://cheerpsy:cheerpsy@localhost:5432/cheerpsy \
  PYTHONPATH=. .venv/bin/python scripts/check_invariants.py

# 前端型別 + 色階守門員
cd apps/web && npx tsc --noEmit && node scripts/check-tokens.mjs
```

- 開發資料庫是 `cheerpsy`，測試資料庫是 `cheerpsy_test`，**不要搞混**。
- 資料全部是假資料，可以自由重建；但改動前仍要先看一眼再覆蓋。
- `next build` 會清掉 dev server 的 `.next`，要跑之前先停掉 preview。

---

## 4 · 兩條不可違反的邊界

**依賴反轉**：`app/routers/`（主系統）只能 import `app/funding/`，
**不可 import `app.institution.*`**。機構方案的變異全部收在子系統裡，
主系統只存報價快照（07 §2.1：存的是當時的答案，歷史金額是事實不是重算的）。

**顏色只表達狀態，不表達操作**（v7 定案 ③）。操作鍵一律中性黑白；
顏色留給「未收／未到／逾期」這種狀態。角色、付費別、年齡層這類**屬性**
也不可借用狀態色——綠色的「管理員」會被讀成已完成。

---

## 5 · 反覆出現的三種錯誤

寫新程式碼前先想一遍，這三種佔了目前修掉的問題的絕大多數：

1. **文件早就寫了、動手前沒讀。** 三條前端契約沒有一條是新原則，
   每條都能在 01／02／06 找到原文。
2. **會把資料走壞的路，常常正是最自然的那條。** 修法是把自然的那條路
   *變成*對的那條路，而不是寫一份「請不要這樣做」的說明；同時在後端加閘門，
   讓錯誤路徑明確失敗而不是靜靜成功。
3. **同一份知識抄成多份，一定會分岔。** 能匯出就匯出；不能匯出就用測試
   把兩邊釘在一起——斷言的不是「回了什麼」，而是兩份清單真的對齊。
