{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter booking exec prisma generate && pnpm --filter booking build"
  },
  "deploy": {
    "preDeployCommand": "cd ../.. && pnpm --filter booking exec prisma migrate deploy",
    "startCommand": "cd ../.. && pnpm --filter booking start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
