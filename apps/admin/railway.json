{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter admin exec prisma generate && pnpm --filter admin build"
  },
  "deploy": {
    "preDeployCommand": "pnpm --filter admin exec prisma migrate deploy",
    "startCommand": "pnpm --filter admin start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
