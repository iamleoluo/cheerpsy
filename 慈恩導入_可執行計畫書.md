# 慈恩導入計畫 — 可執行實作計畫書

## Context

慈恩心理治療所要從 Excel 作業流程遷移至 CheerPsy 系統。原始構想文件（`慈恩導入計畫書.html`）定義了 13 個章節的完整規格，包含欄位對應、差距分析、編號規格、角色權限、核銷案設計、收據格式、四階段導入計畫等。

本文件是對照現有程式碼後產出的**可執行開發計畫**，列出每一步要改什麼檔案、加什麼欄位、寫什麼邏輯。

---

## 現況概要

### 已實作
- 後端 60+ endpoints（auth, cases, appointments, ledger, invoices, rooms, institutions, petty-cash, payouts, reports, reminders, churn, audit, export）
- 前端 14 頁（dashboard, cases, appointments, rooms, ledger, reminders, reconciliation, payouts, petty-cash, reports, churn, institutions, admin/users, guide）
- T+1 日結、70/30 拆帳（**硬編碼 0.7**）、鎖定/解鎖、CSV 匯出、FullCalendar

### 缺口（需本次實作）
1. `staff` 角色 + 權限矩陣
2. 每人不同抽成比例 `commission_rate`（取代硬編碼 0.7）
3. 兩階段個案編號（`temp_seq` / `case_number`）
4. 預約到訪序號 `visit_seq`
5. 結帳週期 `billing_cycle`
6. 機構代號 `institutions.code`
7. 核銷案 `claim_batches`（全新模組）
8. 心理師諮商紀錄確認流程
9. 收據/請款單 PDF 產出
10. 各表 `created_by` 審計欄位
11. 預約自動鎖定（T+1 時鎖入）
12. 資料遷移腳本強化

---

## Phase 1：基礎擴充（staff 角色、抽成比例、created_by）

### 1.1 Alembic Migration：staff 角色 + commission_rate

**新檔**：`apps/api/alembic/versions/xxxx_add_commission_rate.py`

```
ALTER TABLE users ADD COLUMN commission_rate NUMERIC(4,2);
-- role 是 String(20)，不需 DDL 改動，只需改 app 層驗證
```

**改檔**：

| 檔案 | 改動 |
|------|------|
| `apps/api/app/models/user.py:14-16` | 加 `commission_rate = Column(Numeric(4, 2), nullable=True)`；role 註解更新含 staff |
| `apps/api/app/routers/auth.py` | role 驗證加入 `"staff"`；新增 `PUT /users/{user_id}/commission-rate`（admin-only） |
| `apps/api/app/schemas/auth.py` | `UserResponse` 加 `commission_rate: float \| None` |

### 1.2 Alembic Migration：created_by 欄位

**新檔**：`apps/api/alembic/versions/xxxx_add_created_by.py`

```
ALTER TABLE cases ADD COLUMN created_by INTEGER REFERENCES users(id);
ALTER TABLE appointments ADD COLUMN created_by INTEGER REFERENCES users(id);
ALTER TABLE session_records ADD COLUMN created_by INTEGER REFERENCES users(id);
ALTER TABLE invoices ADD COLUMN created_by INTEGER REFERENCES users(id);
ALTER TABLE therapist_payouts ADD COLUMN created_by INTEGER REFERENCES users(id);
ALTER TABLE petty_cash ADD COLUMN created_by INTEGER REFERENCES users(id);
ALTER TABLE institutions ADD COLUMN created_by INTEGER REFERENCES users(id);
```

**改檔**：

| 檔案 | 改動 |
|------|------|
| `apps/api/app/models/case.py` | 加 `created_by = Column(Integer, ForeignKey("users.id"), nullable=True)` |
| `apps/api/app/models/appointment.py` | 同上 |
| `apps/api/app/models/session_record.py` | 同上 |
| `apps/api/app/models/invoice.py` | 同上 |
| `apps/api/app/models/therapist_payout.py` | 同上 |
| `apps/api/app/models/petty_cash.py` | 同上 |
| `apps/api/app/models/institution.py` | 同上 |
| 各對應 router（cases, appointments, invoices, petty_cash, payouts, institutions） | create 時設 `created_by=user.id` |

### 1.3 抽成比例取代硬編碼 0.7

需改的 **6 處硬編碼**：

| 檔案 | 行號 | 內容 | 改法 |
|------|------|------|------|
| `apps/api/app/routers/appointments.py` | 58 | `therapist_share=round(float(a.amount) * 0.7, 2)` | 查 therapist 的 `commission_rate`，fallback 0.70 |
| `apps/api/app/routers/ledger.py` | 54-55 | `therapist_share / clinic_share` 用 0.7 / 0.3 | 用 `r.commission_rate_used`，fallback 0.70 |
| `apps/api/app/routers/payouts.py` | 84 | `payout_details()` 中 `* 0.7` | 用 `sr.commission_rate_used`，fallback 0.70 |
| `apps/api/app/routers/payouts.py` | 125 | `generate_payouts()` 中 `* 0.7` | 同上 |
| `apps/api/app/routers/reports.py` | 35-36 | `monthly_report()` 總計用 0.7 | 用 `r.commission_rate_used`，fallback 0.70 |
| `apps/api/app/routers/reports.py` | 78 | 個別心理師 share 用 0.7 | 同上 |

**改 settlement service**（`apps/api/app/services/settlement.py:40-51`）：
- 建立 `SessionRecord` 時查 `User.commission_rate`
- 寫入 `commission_rate_used`（snapshot）
- `session_records` model 加 `commission_rate_used = Column(Numeric(4, 2), nullable=True)`

### 1.4 Staff 角色權限

**改檔**：

| 檔案 | 改動 |
|------|------|
| `apps/api/app/routers/cases.py` | `create_case` 的 `RequireRole` 加 `"staff"` |
| `apps/api/app/routers/appointments.py` | `create_appointment` 加 `"staff"` |
| `apps/api/app/routers/ledger.py` | 收款標記加 `"staff"`；鎖定/解鎖維持 admin only |
| `apps/api/app/routers/institutions.py` | 維持 admin only |
| `apps/api/app/routers/payouts.py` | `generate_payouts` / `mark_paid` 維持 admin only |
| `apps/api/app/routers/petty_cash.py` | 新增加 `"staff"`；刪除維持 admin only |
| `apps/web/src/components/sidebar.tsx` | `roleLabel` 加 `staff: "行政人員"`；相關 nav items 的 `roles` 陣列加 `"staff"` |
| `apps/web/src/app/(app)/admin/users/page.tsx` | `roleLabels` 加 `staff`；邀請選單加 `<option value="staff">`；加 commission_rate 顯示/編輯 |

### 1.5 Phase 1 驗證

- [ ] 建立 staff 角色使用者，驗證邀請 → 註冊 → 登入 → sidebar 顯示正確
- [ ] 設定心理師 commission_rate = 0.80，觸發日結，確認 session_record.commission_rate_used = 0.80
- [ ] 確認 payouts 月結使用 per-therapist rate
- [ ] 確認 reports 月報數字使用 commission_rate_used
- [ ] 確認 staff 不能存取 admin-only 端點

---

## Phase 2：兩階段個案編號 + 到訪序號

### 2.1 Alembic Migration

**新檔**：`apps/api/alembic/versions/xxxx_add_case_numbering.py`

```sql
-- cases
CREATE SEQUENCE cases_temp_seq_seq;
ALTER TABLE cases ADD COLUMN temp_seq INTEGER DEFAULT nextval('cases_temp_seq_seq');
ALTER TABLE cases ADD COLUMN case_number VARCHAR(10) UNIQUE;
ALTER TABLE cases ADD COLUMN billing_cycle VARCHAR(20) NOT NULL DEFAULT 'once';

-- appointments
ALTER TABLE appointments ADD COLUMN visit_seq INTEGER;
```

### 2.2 後端邏輯

**改檔**：

| 檔案 | 改動 |
|------|------|
| `apps/api/app/models/case.py` | 加 `temp_seq`、`case_number`、`billing_cycle` 欄位 |
| `apps/api/app/models/appointment.py` | 加 `visit_seq` 欄位 |

**新檔**：`apps/api/app/services/case_numbering.py`

```python
def generate_case_number(db, case) -> str:
    """
    格式: {YY}{SEQ4}{末2碼}
    - YY = 民國年（西元 - 1911）
    - SEQ4 = 該年度流水號 0001~9999
    - 末2碼 = 身分證字號最後 2 碼（數字）
    """
    yy = str(datetime.now().year % 100).zfill(2)  # 2026 → "26"

    # 查該年度已發出的 case_number 數量
    count = db.query(Case).filter(Case.case_number.like(f"{yy}%")).count()
    seq = str(count + 1).zfill(4)

    # 解密國民身分證取末 2 碼
    last2 = decrypt_national_id(case.national_id_encrypted)[-2:]

    return f"{yy}{seq}{last2}"
```

**改 `apps/api/app/routers/cases.py`**：

- `create_case()`：自動分配 `temp_seq`（`SELECT COALESCE(MAX(temp_seq), 0) + 1 FROM cases`）
- `update_case()`：當 status 從 `initial` → `ongoing` 時：
  - 檢查 `national_id_encrypted` 已填入
  - 呼叫 `generate_case_number()` 寫入 `case_number`
  - `case_number` 一旦寫入不可修改
- 新增 `POST /cases/{case_id}/activate` 專用端點（更明確的轉換入口）
- `_to_response()` 加入 `temp_seq`、`case_number`、`billing_cycle`

**改 `apps/api/app/routers/appointments.py`**：

- `create_appointment()`：查 `SELECT COALESCE(MAX(visit_seq), 0) + 1 FROM appointments WHERE case_id = :cid`，寫入 `visit_seq`
- `create_batch()`：同上邏輯，batch 內依序遞增
- `_to_response()`：回傳 `visit_seq`，顯示格式 `{case_number}-{visit_seq:03d}`

### 2.3 前端

**改檔**：

| 檔案 | 改動 |
|------|------|
| `apps/web/src/app/(app)/cases/page.tsx` | 列表顯示 `#0001`（Stage 1）或正式編號（Stage 2）；新增表單加 `billing_cycle` 下拉；加「轉正式」按鈕觸發 activate |
| `apps/web/src/app/(app)/appointments/page.tsx` | 列表加 `visit_seq` 欄位 |

### 2.4 Phase 2 驗證

- [ ] 新增個案：確認 temp_seq 自動遞增，case_number 為 null
- [ ] 填入身份證後觸發 activate：確認 case_number 格式正確且不可再改
- [ ] 無身份證時觸發 activate：確認被拒絕
- [ ] 新增預約：確認 visit_seq 正確遞增
- [ ] 批次新增預約：確認 visit_seq 依序分配

---

## Phase 3：核銷案系統（claim_batches）+ 機構代號

### 3.1 Alembic Migration

**新檔**：`apps/api/alembic/versions/xxxx_add_claim_batches.py`

```sql
-- institutions 加代號
ALTER TABLE institutions ADD COLUMN code VARCHAR(5) UNIQUE;

-- session_records 加核銷案關聯 + 心理師確認
ALTER TABLE session_records ADD COLUMN claim_batch_id INTEGER REFERENCES claim_batches(id);
ALTER TABLE session_records ADD COLUMN therapist_doc_submitted_at TIMESTAMPTZ;
ALTER TABLE session_records ADD COLUMN therapist_doc_submitted_by INTEGER REFERENCES users(id);

-- 新表
CREATE TABLE claim_batches (
    id SERIAL PRIMARY KEY,
    batch_number VARCHAR(50) UNIQUE NOT NULL,
    external_ref VARCHAR(100),
    type VARCHAR(20) NOT NULL,           -- self_pay / institution
    billing_cycle VARCHAR(20),           -- snapshot from case
    expected_sessions INTEGER,
    institution_id INTEGER REFERENCES institutions(id),
    case_id INTEGER REFERENCES cases(id),
    period_start DATE,
    period_end DATE,
    total_amount NUMERIC(10,2) DEFAULT 0,
    payment_method VARCHAR(20),
    payment_note VARCHAR(200),
    status VARCHAR(20) NOT NULL DEFAULT 'collecting',
    created_by INTEGER REFERENCES users(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 3.2 後端 Model + Router

**新檔**：

| 檔案 | 內容 |
|------|------|
| `apps/api/app/models/claim_batch.py` | ClaimBatch SQLAlchemy model |
| `apps/api/app/schemas/claim_batch.py` | ClaimBatchCreate / ClaimBatchResponse / ClaimBatchUpdate |
| `apps/api/app/routers/claim_batches.py` | 核銷案 CRUD + 狀態流轉 |
| `apps/api/app/services/claim_batch.py` | 編號產生、金額加總、readiness 檢查 |

**Router endpoints（`/claim-batches`）**：

```
GET  /claim-batches                     列表（filter: status, case_id, institution_id, month）
GET  /claim-batches/{id}                明細（含 linked session_records）
POST /claim-batches                     建立核銷案，傳入 session_record_ids
PUT  /claim-batches/{id}                更新 external_ref / payment info
PUT  /claim-batches/{id}/submit         → submitted
PUT  /claim-batches/{id}/receive        → received（機構案）
PUT  /claim-batches/{id}/close          → closed（自費案）
POST /claim-batches/{id}/records        追加 session_records
DELETE /claim-batches/{id}/records/{rid} 移除 session_record
GET  /claim-batches/{id}/receipt        下載收據 PDF（Phase 4）
GET  /claim-batches/{id}/claim-form     下載請款單 PDF（Phase 4）
```

**核銷案編號產生規則**：

| 類型 | 格式 | 範例 |
|------|------|------|
| 自費 | `S{週期碼}-{case_number}-{YYYYMM}` | `SM-26000145-202601` |
| 機構 | `I-{institutions.code}-{YYYYMM}` | `I-NTC01-202601` |

週期碼對應：`once` → `O`、`monthly` → `M`、`multiple` → `X`

**狀態機**：

```
自費：submitted → closed
機構：collecting → ready → submitted → received
```

- `collecting → ready`：自動觸發（所有 session_records 的 therapist_doc_submitted_at 都有值）
- 其餘由 staff/admin 手動操作

### 3.3 心理師諮商紀錄確認

**改 `apps/api/app/routers/ledger.py`**：

新增端點 `PUT /ledger/{record_id}/confirm-doc`：
- 僅限 therapist（且為該 record 的負責心理師）或 admin
- 寫入 `therapist_doc_submitted_at = now()`、`therapist_doc_submitted_by = user.id`
- 寫入後檢查同 claim_batch 內所有 records 是否都已確認 → 自動升 batch status 到 `ready`

### 3.4 機構代號

**改檔**：

| 檔案 | 改動 |
|------|------|
| `apps/api/app/models/institution.py` | 加 `code = Column(String(5), unique=True, nullable=True)` |
| `apps/api/app/routers/institutions.py` | create/update 時處理 `code`；新增 `PUT /institutions/{id}` 編輯端點 |

### 3.5 前端：核銷案管理頁面

**大幅改寫 `apps/web/src/app/(app)/reconciliation/page.tsx`**：

原本是唯讀對帳報表，改為核銷案管理工作台：

```
Tab 1：核銷案列表
  - 狀態 filter（collecting / ready / submitted / received / closed）
  - 每列顯示：編號、類型、個案/機構名、期間、金額、狀態、操作按鈕
  - 點擊展開明細 → 顯示 linked session_records + 心理師確認狀態

Tab 2：建立核銷案
  - Step 1：選個案（自費）或選機構（機構案）
  - Step 2：顯示可勾選的 session_records（未歸屬核銷案的）
  - Step 3：設定期間、billing_cycle → 建立

Tab 3：月對帳（保留原有的 reconciliation summary）
```

**改 `apps/web/src/app/(app)/ledger/page.tsx`**：
- session_record 列加「確認文件」按鈕（therapist 角色看到，綁定自己的 records）
- 顯示 claim_batch_id 連結

**改 `apps/web/src/app/(app)/institutions/page.tsx`**：
- 加 `code` 欄位顯示/編輯

### 3.6 Phase 3 驗證

- [ ] 建立自費核銷案：勾選 records → 金額自動加總 → 編號正確
- [ ] 建立機構核銷案：status = collecting
- [ ] 心理師確認文件 → 全部確認後 batch 自動升為 ready
- [ ] staff 提交 → submitted → 填 external_ref
- [ ] admin 標記 received / closed
- [ ] 同一 record 不能同時屬於兩個 batch
- [ ] 核銷案列表 filter 正確

---

## Phase 4：自動鎖定、PDF 產出、資料遷移強化

### 4.1 T+1 自動鎖定

**改 `apps/api/app/services/settlement.py:40-51`**：

在建立 SessionRecord 時同時設定 `locked_at = datetime.now(timezone.utc)`。
效果：日結後的 record 自動鎖定，只有 admin 可解鎖。

### 4.2 收據/請款單 PDF

**新增依賴**：`apps/api/requirements.txt` 加 `reportlab`

**新檔**：`apps/api/app/services/pdf_generator.py`

```python
def generate_self_pay_receipt(batch: ClaimBatch, records, case, ...) -> bytes:
    """格式 A：自費個案收據"""
    # 收據編號、個案姓名、心理師、日期
    # 明細表：諮商日期 / 心理師 / 服務項目 / 金額
    # 合計金額、簽名欄

def generate_institution_claim_form(batch: ClaimBatch, records, institution, ...) -> bytes:
    """格式 B：機構請款單"""
    # 請款單號、機構名稱、期間
    # 明細表：個案姓名 / 諮商日期 / 心理師 / 服務項目 / 諮商類型 / 金額
    # 個案數、場次數、合計金額、簽名欄
```

**改 `apps/api/app/routers/claim_batches.py`**：
- `GET /claim-batches/{id}/receipt` → StreamingResponse(PDF)
- `GET /claim-batches/{id}/claim-form` → StreamingResponse(PDF)

**前端**：
- `reconciliation/page.tsx` 核銷案明細加「下載收據」/「下載請款單」按鈕

### 4.3 資料遷移腳本強化

**改 `apps/api/import_excel.py`**（或新建 `apps/api/scripts/import_*.py`）：

1. **個案匯入**：
   - 民國年 → 西元自動轉換
   - 機構名 → 查 institutions 表取 id
   - 心理師姓名 → 查 users 表取 id
   - Stage 1 個案：只設 temp_seq
   - Stage 2 個案：產生 case_number
   - 設 `created_by` = 執行匯入的 admin id

2. **行政月報表匯入**：
   - 每筆 → appointment (status=executed) + session_record
   - 歷史時段缺失 → 預設 09:00-10:00
   - commission_rate_used 從 users.commission_rate 帶入
   - visit_seq 依歷史日期排序後逐筆計算

3. **匯款資料匯入**：
   - 比對 session_record（by 心理師 + 個案 + 金額 + 日期）
   - 更新 payment_method=transfer、payment_note=後5碼、receipt_number

4. **零用金匯入**：
   - 篩選「雜支」品項 → petty_cash
   - category 依內容手動對應

5. **驗證腳本**：每步匯入後自動核對筆數 + 金額加總

### 4.4 Phase 4 驗證

- [ ] 日結後 session_record 自動帶 locked_at
- [ ] 下載自費收據 PDF：格式、金額、簽名欄正確
- [ ] 下載機構請款單 PDF：跨個案彙整、場次數正確
- [ ] 匯入腳本：個案筆數 match、金額差異 < 1%
- [ ] 端到端流程：匯入資料 → 建核銷案 → 下載 PDF

---

## 檔案異動總覽

### 新增檔案（~10 個）

| 檔案 | 用途 |
|------|------|
| `apps/api/alembic/versions/xxxx_add_commission_rate.py` | Phase 1 migration |
| `apps/api/alembic/versions/xxxx_add_created_by.py` | Phase 1 migration |
| `apps/api/alembic/versions/xxxx_add_case_numbering.py` | Phase 2 migration |
| `apps/api/alembic/versions/xxxx_add_claim_batches.py` | Phase 3 migration |
| `apps/api/app/models/claim_batch.py` | ClaimBatch model |
| `apps/api/app/schemas/claim_batch.py` | Pydantic schemas |
| `apps/api/app/routers/claim_batches.py` | 核銷案 API |
| `apps/api/app/services/claim_batch.py` | 核銷案 business logic |
| `apps/api/app/services/case_numbering.py` | 個案編號產生 |
| `apps/api/app/services/pdf_generator.py` | PDF 收據產生 |

### 修改檔案（~20 個）

| 檔案 | Phase | 改動摘要 |
|------|-------|---------|
| `apps/api/app/models/user.py` | 1 | +commission_rate |
| `apps/api/app/models/case.py` | 1,2 | +created_by, +temp_seq, +case_number, +billing_cycle |
| `apps/api/app/models/appointment.py` | 1,2 | +created_by, +visit_seq |
| `apps/api/app/models/session_record.py` | 1,3 | +created_by, +commission_rate_used, +claim_batch_id, +therapist_doc_* |
| `apps/api/app/models/invoice.py` | 1 | +created_by |
| `apps/api/app/models/therapist_payout.py` | 1 | +created_by |
| `apps/api/app/models/petty_cash.py` | 1 | +created_by |
| `apps/api/app/models/institution.py` | 1,3 | +created_by, +code |
| `apps/api/app/services/settlement.py` | 1,4 | +commission_rate_used snapshot, +locked_at |
| `apps/api/app/routers/auth.py` | 1 | staff role validation, commission-rate endpoint |
| `apps/api/app/routers/cases.py` | 1,2 | +created_by, +activate endpoint, +temp_seq/case_number |
| `apps/api/app/routers/appointments.py` | 1,2 | +created_by, +visit_seq, 移除 0.7 硬編碼 |
| `apps/api/app/routers/ledger.py` | 1,3 | 移除 0.7 硬編碼, +confirm-doc endpoint, staff 權限 |
| `apps/api/app/routers/payouts.py` | 1 | 移除 0.7 硬編碼, +created_by |
| `apps/api/app/routers/reports.py` | 1 | 移除 0.7 硬編碼 |
| `apps/api/app/routers/institutions.py` | 1,3 | +created_by, +code, +PUT 編輯端點 |
| `apps/api/app/routers/petty_cash.py` | 1 | +created_by, staff 權限 |
| `apps/api/app/main.py` | 3 | 註冊 claim_batches router |
| `apps/web/src/components/sidebar.tsx` | 1 | +staff role label + nav visibility |
| `apps/web/src/app/(app)/admin/users/page.tsx` | 1 | +staff 選項, +commission_rate 顯示/編輯 |
| `apps/web/src/app/(app)/cases/page.tsx` | 2 | +temp_seq/case_number 顯示, +billing_cycle, +activate 按鈕 |
| `apps/web/src/app/(app)/appointments/page.tsx` | 2 | +visit_seq 顯示 |
| `apps/web/src/app/(app)/reconciliation/page.tsx` | 3,4 | **大改**：核銷案管理 + PDF 下載 |
| `apps/web/src/app/(app)/ledger/page.tsx` | 3 | +confirm-doc 按鈕, +claim_batch_id 顯示 |
| `apps/web/src/app/(app)/institutions/page.tsx` | 3 | +code 欄位 |

---

## 依賴關係圖

```
Phase 1 ─── staff role + commission_rate + created_by
  │
  ├── Phase 2 ─── case_number + visit_seq + billing_cycle
  │     │
  │     └── Phase 3 ─── claim_batches + institution code + therapist doc confirm
  │           │
  │           └── Phase 4 ─── auto-lock + PDF + migration scripts
  │
  └── (Phase 1 完成後可同步進行 Phase 2)
```

Phase 1 → Phase 2 → Phase 3 → Phase 4 嚴格依序。每個 Phase 完成後跑一次驗證清單再進下一步。

---

## 待確認事項（執行前需決定）

| # | 議題 | 影響 |
|---|------|------|
| D2 | 歷史個案匯入範圍（全部 or 僅 115 年進行中） | Phase 4 遷移腳本範圍 |
| D4 | 歷史諮商記錄預設時段（09:00-10:00？） | Phase 4 匯入邏輯 |
| D5 | 系統正式切換日（建議 7/1） | Phase 4 排程 |
| ~~YY~~ | ~~case_number 年份碼~~ → **已決定：西元 2 碼**（2026 → "26"），case_number = `26000145`，共 8 碼 | Phase 2 |
